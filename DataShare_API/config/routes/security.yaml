_security_logout:
    resource: security.route_loader.logout
    type: service

# No controller: the firewall intercepts the request before routing reaches one,
# checks the credentials and returns the token. The route only has to exist so
# that json_login can point at it.
api_login:
    path: /api/login
    methods: [POST]
